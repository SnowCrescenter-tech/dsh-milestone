# Roadmap (dsh-milestone)

版本路线图。**顺序执行，前一个版本发布后再开始下一个**（防止并行改同一批文件造成冲突）。

## v0.6.2 — 进行中（当前批次）

- [x] 设置按钮进展开队列（折叠态只留箭头）
- [x] 设置改为居中模态框 + 功能悬停描述（中英） + 底部推广区改为紧凑卡片网格
- [x] 移除圆点间 turn 分隔线（改为组间距微增）
- [x] 徽章由实线圆环改为柔边光晕
- [x] 个性化模块：强调色（预设+自定义）/ 图标与圆点大小 / 距侧边距离 / 位置左或右（浮层自动换侧）
- [x] README 重构（分组总览表 + 折叠已知限制/更新日志）
- [x] npm tarball 纳入 assets/*.svg（npm 页 README 图片修复）：npm 渲染器不重写相对路径，已改 absolute raw URLs（0.6.3 发布后 npm 页可见）
- [x] **设置内语言切换（zh/en）**：设置模态加「语言 Language」区块，选项：跟随系统（harness 界面语言）/ 中文 / English，持久化到偏好；强制时以本地 zh/en 词典渲染（现有 locales.ts 双语文档已齐全，只差切换与读取路径）。★ 此项在 v0.6.2 集成时由主线程落地（避免与模态改造并发冲突）
- [x] 版本门禁 + 按 RELEASING.md 发布（npm latest=0.6.2，GitHub Release v0.6.2，UTF-8 正文验证无乱码）

## v0.6.3 — 聚焦模式高级设置（排队，勿提前开工）

- 设置里「聚焦」展开为高级设置：可自定义聚焦/淡化哪些内容及其强度：
  - think（推理）区：淡化 / 折叠（做成可展开的小块）
  - 工具调用块：淡化 / 折叠
  - 组合自由搭配（多选 + 强度档位）
- 实现前提调研（开工时做）：工具调用卡片（tool/call、tool/result）在 rc.2 ui-conversation 里的稳定 DOM 标记（现在只锚定 `data-variant="think"`）；淡化=CSS 规则注入（现 FOCUS_CSS 模式），折叠=需要容器交互（参考 `[data-open]` 契约）
- 持久化并入 toolbar 偏好；i18n 双语文案

## v0.6.4 — 新手教程（已完成，随 v0.6.4 发布）

- [x] 首次使用触发（任意安装渠道：npm / GitHub / link）：**第一次安装后的第一次可用会话**，插件首次渲染且无全局标记（`dsh-milestone.onboarded`，跨会话共享，不按会话隔离）时弹出引导；**印象即持久化**（欢迎页一显示立即写标记，中途关页/刷新/崩溃都不重弹；想再看走设置里的「重新查看教程」）
- [x] 元素（按行业惯例必须齐全）：
  - [x] 欢迎页：一句话说明 + 「开始引导 / 跳过」
  - [x] 步骤页：4 步教学（圆点时间线演示、搜索与收藏演示、个性化与设置图例、更新检测与支持），内置 mock 数据迷你圆点条，不依赖真实会话
  - [x] 进度指示（第 n/4 步 + 进度点）、上一步/下一步、**跳过（Skip）随时可点**
  - [x] 持久化（`dsh-milestone.onboarded`，印象即写）；设置内「重新查看教程」入口
  - [x] 模态内 self-contained，不用易碎的 coach-mark 定位
- [x] i18n 双语文案；测试覆盖（触发条件 / skip / 持久化 / 重开）— 385/385 全绿

## v0.6.5 — 视觉修复 + 真实组件教练教程（已完成，随 v0.6.5 发布）

- [x] **设置模态白色背景修复**（UA 默认按钮底色漏出：基础态补 `background: transparent`，对比度恢复；Playwright 计算色实测验证）
- [x] **新手教程改为真实组件联动（coach-mark）**：删除假演示模态；气泡锚定真实元素（展开箭头 / 设置齿轮 / 圆点 / 更新检测），目标高亮环 + 就近气泡（含完整 tip 观感）；「点这里」→ 用户真实点击 → 教程观察状态自动推进（下一步兜底）；进度点 + 上一步 / 下一步 / 随时跳过 + Esc；印象即持久化与设置重开语义不变；气泡在设置模态打开时挂起；全双语 — 386/386 全绿

## 流程约定

每个版本发布走 [RELEASING.md](./RELEASING.md)：门禁 → dry-run 核对 Tarball（README 引用的 assets 必须在内）→ UTF-8 Release 正文（文件读取 + 显式字节 + 回读验证）。